\usepackage[margin=1in]{geometry}

\usepackage{fontspec}
\setmainfont[Numbers=OldStyle]{Junicode}

\newfontface{\num}[Numbers=Uppercase]{Junicode}
\newfontface{\url}{Ubuntu Mono}

\usepackage{nicefrac}
\newcommand{\nf}[2]{\num{\nicefrac{#1}{#2}}}

\newenvironment{recipe}[3][]{% provide name and portions
	\noindent{\LARGE #2} \hspace{\fill} #3 portions
	\hrule \vspace{2pt}	\hspace{\fill} {\footnotesize #1}
	\vspace{12pt}
	
	\noindent
}{%
	%PUT A VSPACE HERE *BEFORE* the next title somehow
}

\newenvironment{step}[0]{%
	\vspace{\parskip}\hspace{2em}\noindent
	\begin{minipage}{\linewidth}
}{%
\end{minipage}%
\vspace{10pt}\noindent%
}

\newenvironment{ingrs}[0]{%
	\begin{minipage}[t]{.4\linewidth}%
		\setlength{\tabcolsep}{2pt}
		\begin{tabular}[t]{r p{4em} l}%This is hardcoded, which sucks, but is possibly inevitable since I can't define a variable
}{%
		\end{tabular}%
	\end{minipage}%
}

\newcommand{\ingr}[3]{#1 & #2 & #3 \\}
\newcommand{\itemingr}[2]{#1 & \multicolumn{2}{l}{#2}\\}
\newcommand{\tool}[1]{\multicolumn{3}{l}{#1}\\}

\newenvironment{stepdesc}[0]{%
	\begin{minipage}[t]{.5\linewidth}
}{%
	\end{minipage}
}